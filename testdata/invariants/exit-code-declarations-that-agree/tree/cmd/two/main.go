// Command two is a fixture. It declares the same two codes as command one,
// with the same numbers, which is the whole of what this leg asks for.
package main

const (
	exitClean   = 0
	exitRefused = 1
)

func main() {}
