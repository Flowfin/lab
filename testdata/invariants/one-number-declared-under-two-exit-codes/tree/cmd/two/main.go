// Command two is a fixture. It gives the number command one already uses a
// second name, so the contract carries one code answering to two of them and
// neither declaration is wrong about itself.
package main

const (
	exitClean  = 0
	exitDenied = 1
)

func main() {}
